*** Variables ***
${apiBaseUrl}    http://127.0.0.1:8000
${ACCESS_TOKEN}    ${EMPTY} # Suite variable to store the access token
${loginSession}    login_session
${userSession}    user_session
${username}    admin
${password}    111111@A